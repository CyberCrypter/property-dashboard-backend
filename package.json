{
  "name": "property-dashboard-backend",
  "version": "1.0.0",
  "description": "Backend API for Property Dashboard",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0",
    "dev": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 